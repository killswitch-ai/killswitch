[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "killswitch-ai"
version = "0.1.6"
description = "Local LLM egress control for Python developers — prevents API keys, secrets, .env files, and sensitive payloads from being sent to LLM APIs"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "Yodabyte LLC" }]
keywords = ["llm", "security", "openai", "anthropic", "egress", "dlp", "secrets"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
openai = ["openai>=1.0"]
anthropic = ["anthropic>=0.20"]
all = ["openai>=1.0", "anthropic>=0.20"]
mcp = ["mcp>=1.0.0"]
dev = [
    "pytest>=7.0",
    "pytest-cov",
    "openai>=1.0",
    "anthropic>=0.20",
    "mcp>=1.0.0",
]

[project.scripts]
killswitch = "killswitch.cli.main:main"
killswitch-mcp = "killswitch.mcp.server:serve"

[project.urls]
Homepage = "https://github.com/killswitch-ai/killswitch"
Documentation = "https://killswitch-ai.com"
"Bug Tracker" = "https://github.com/killswitch-ai/killswitch/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/killswitch"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

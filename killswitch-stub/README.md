# killswitch (deprecated)

This package has moved to **[killswitch-ai](https://pypi.org/project/killswitch-ai/)**.

Please update your dependency:

```bash
pip install killswitch-ai
```

This stub package exists only to redirect users and will install `killswitch-ai` automatically as a dependency.
